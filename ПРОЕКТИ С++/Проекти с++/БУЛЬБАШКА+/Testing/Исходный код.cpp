#include <iostream>
#include <conio.h>
#include <cmath>

using namespace std;

void main()
{
	char str[100];
	while(int x = 1)
	{
		cout << "Enter string: ";
		cin >> str;

		if(x = strlen(str)) break;
	
	}
	_getch();
}