#include <conio.h>
#include <iostream>
#include <cmath>
#include <string>

using std ::cout;
using std ::endl;
using std ::cin;

void main()
{
setlocale(LC_ALL, "Ukrainian");
cout << "\t\t New Program \n\n" << endl;
/*************************************************
cout << "Enter N: ";
int N, S;
S = 0;
cin >> N;
for(int i = 1; i <= N; i++)
{
	S += i;
}
int F = 1;
for (int j = 1; j <= N; j++)
{
	F *= j;
}
cout << "Symma = " << S << endl;
cout << "Dobytok = " << F << endl;
	*************************************************/
/*********************************************************************************
int A[10];
	cout << "Заповнення масиву:\n";
	for(int i = 0; i < 10; i++)
	{
		cout << "Введiть значення " << i << " елемента масиву: ";
		while (!(cin >> A[i]))
		{
			cin.clear();
			while(cin.get() != '\n');
			cout << "Введiть значення " << i << " елемента масиву: ";
		}
	}
	system("cls");
cout << "\nВихiдний масив:\n";
for (int i = 0; i < 10; i++)
		cout << "A[" << i << "] = " << A[i] << endl;
int i = 0;
int imax = 0;
int j = 0;
for (int i = 0; i < 10; i++)
{
	
		if (A[i] > A [i+1])
	{
		imax = A [i];
		
	}
		j++;
		imax = A [i++];
		cout << "\n\n\nNUMBER = " << j << endl;
}
cout << "\n\nMAX = " << imax << endl;
int R;
A [9] = imax;
cout << "\nВихiдний масив:\n";
for (int i = 0; i < 10; i++)
		cout << "A[" << i << "] = " << A[i] << endl;
	***************************************************************************/
unsigned long A;
A = -1;
cout << A;




	_getch();
}